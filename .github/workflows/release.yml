name: Release CI

on:
  push:
    tags:
      - v*

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: setup requirements
      run: |
        sudo apt update
        sudo apt install bsdtar
    - name: make
      run: make
    - name: Create release
      id: create_release
      uses: actions/create-release@v1.0.0
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        body: |
          [![downloads](https://img.shields.io/github/downloads/yuk7/playstore-excl-upd/${{ github.ref }}/total?style=flat-square)](https://github.com/yuk7/playstore-excl-upd/releases/download/${{ github.ref }}/PEU.zip)
        draft: false
        prerelease: false
    - name: Upload Release Asset
      id: upload-release-asset 
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: PEU.zip
        asset_name: PEU_${{github.ref}}.zip
        asset_content_type: application/zip